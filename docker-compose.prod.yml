services:
  adminnginx:
    image: ${ADMINNGINX_IMAGE:-ghcr.io/seah78/adminnginx:latest}
    container_name: adminnginx
    env_file:
      - .env
    environment:
      ADMINNGINX_DEPLOYMENT_NAME: ${ADMINNGINX_DEPLOYMENT_NAME:-non-configure}
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - /opt/nginx_proxy/nginx-config:/nginx-config
      - /opt/nginx_proxy/html:/nginx-html
      - /opt/nginx_proxy/letsencrypt:/letsencrypt
      - /opt:/host/opt
      - /var/run/docker.sock:/var/run/docker.sock
      - ${DOCKER_CONFIG_PATH:-/root/.docker}:/root/.docker:ro
    networks:
      - internal_network
    expose:
      - "8000"
    restart: unless-stopped

networks:
  internal_network:
    external: true
